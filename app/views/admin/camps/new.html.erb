<%= form_for [:admin, @camp] do |f| %>
  <table id="show-camp-table">
    <tr>
      <th><%= f.label "Location" %></th>
      <td><%= f.text_field :location, autofocus: true, required: true %> </td>
    </tr>
  </table>

  <%= f.submit "Create", disable_with: 'Adding location...' %>
<% end %>

<%= link_to "Back", admin_camps_path %>
