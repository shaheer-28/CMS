<%= link_to "Users Listing", admin_users_path %><br>
<%= link_to "Camp Applications Listing", admin_camps_registrations_path %><br>
<%= link_to "Camps Listing", admin_camps_path %><br><br>
<%= link_to "Add New Location", new_admin_location_path %><br>
<table class="table table-hover">
  <tr>
    <td></td>
    <td>
      <%= form_with method: :get, local: true do |f| %>
        <%= f.text_field :search_key, value: params[:search_key], placeholder: 'Search Locations' %>
        <%= f.submit 'search'%>
      <% end %>
    </td>
  </tr>
  <tr class="thead-dark">
    <th><%= sortable "ID" %></th>
    <th><%= sortable "LOCATION" %></th>
    <th colspan="3"></th>
  </tr>
  <%= render @locations %>
</table>
<%== pagy_nav(@pagy).html_safe %>

<%= link_to 'Export all locations', admin_locations_path(format: :csv) %><br><br>
<%= link_to "Sign out", destroy_user_session_path, method: :delete %>
